
import pytest
import model as model
import relationship as relationships
import UMLClass as UMLClass
from controller import Controller

#we need to set the system varriable for the build system it won't be able to create the gui
#but you can run it locally 
# def test_controller():
#     test=Controller()
